class Train {
  String plate_number, origin, destination, depart_time, arrive_time, type;
  Train({
    this.plate_number,
    this.origin,
    this.destination,
    this.depart_time,
    this.arrive_time,
    this.type,
  });
}